export default {
  animation: true,
  background: `https://unsplash.it/1500/1000?image=${Math.round(Math.random() * 1000)}`,
  transparent: true,
  notifications: {
    // inset: false, // [*]
    background: false,
    border: 1,
    excluded: {},
    mini: false,
    position: 'right-bottom',
    small: false,
    timeout: 5000,
  },
  theme: {
    dark: true,
    name: 'Default',
  },
  remember: true,
  welcomeMessage: true,
  default: {
    animation: true,
    background: `https://unsplash.it/1500/1000?image=${Math.round(Math.random() * 1000)}`,
    transparent: true,
    notifications: {
      // inset: false, // [*]
      background: false,
      border: 1,
      excluded: {},
      mini: false,
      position: 'right-bottom',
      small: false,
      timeout: 5000,
    },
    theme: {
      dark: true,
      name: 'Default',
    },
    remember: true,
    welcomeMessage: true,
  },
};
